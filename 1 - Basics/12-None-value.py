# Describes assigning of None to declaring a variable for future use
# Alternative way of declaring variables since variables have to be declared and 
#       assigned a value during definition in python

spam = None
print(spam) # Returns None
print(type(spam)) # Returns NoneType

print(spam == None)
